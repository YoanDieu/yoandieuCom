<div class="jumbotron" id="jumbotron" style="background:url('<%= pagejumbo %>'); background-attachment:fixed; background-position:center; background-size:cover;">

  <h1 id="pageTitle"><%= pagetitle %></h1>
  <% puts bandtitle %>
  <div class="filter">
    <h2>
      <% if bandtitle %>
        <%= bandtitle %>
      <% else %>
        En construction
      <% end %>
     </h2>
    <p>
      <% if bandcontent %>
        <%== bandcontent %>
      <% else %>
       Bienvenue sur le site de présentation de <strong><bold>Yoan Dieu</bold></strong>, Seigneur du developpement Web/mobile (Junior) ! En formation à <a href='http://www.simplon.co'>Simplon.co</a> !
      <% end %>
    </p>
    <% if credits %>
      <p> <%= credits %> </p>
    <% end %>
  </div>
</div>

<i class="fa fa-chevron-down fa-4x" id="arrow"></i>
